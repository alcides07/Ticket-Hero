import styled from "styled-components";
export const ContainerGeral = styled.div`
    padding: 1em;
    display: flex;
    justify-content: center;
    .card{
        margin: 1em;
    }
`;

export const TituloPagina = styled.span`
    font-weight: 500;
    font-size: large;
    display: inline;
    border-radius: 0.5em;
`

export const ContainerTituloPagina = styled.header`
    width: 100vw;
    margin-top: -3em;
    text-align: center;
`